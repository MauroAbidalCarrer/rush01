diff --git a/check_input.c b/check_input.c
index 847ffc0..a924bef 100644
--- a/check_input.c
+++ b/check_input.c
@@ -1,33 +1,27 @@
-/* ************************************************************************** */
-/*                                                                            */
-/*                                                        :::      ::::::::   */
-/*   check_input.c                                      :+:      :+:    :+:   */
-/*                                                    +:+ +:+         +:+     */
-/*   By: kychoi <marvin@42.fr>                      +#+  +:+       +#+        */
-/*                                                +#+#+#+#+#+   +#+           */
-/*   Created: 2021/08/15 12:17:03 by kychoi            #+#    #+#             */
-/*   Updated: 2021/08/15 12:17:04 by kychoi           ###   ########.fr       */
-/*                                                                            */
-/* ************************************************************************** */
-
+#include <stdlib.h>
 #include "header.h"
-
 int	check_input(char *input)
 {
 	int		i;
 
 	i = 0;
+	/* 31 = 16 letter && 15 space */
 	if (ft_strlen(input) != 31)
 		return (0);
+	/* find each word in input string */
 	while (input[i])
 	{
+		//if pair
 		if (i % 2 == 0)
 		{
+			//if not number(48 ~ 57 in ascii) returns 0;
 			if (!is_number(input[i]))
 				return (0);
+			//if more than 4 returns 0;
 			if ((input[i] - 48) > 4 || (input[i] - 48) < 1)
 				return (0);
 		}
+		/* if impair && not space returns 0 */
 		else if (input[i] != 32)
 			return (0);
 		++i;
diff --git a/header.h b/header.h
index 5afd8e8..0a8a241 100644
--- a/header.h
+++ b/header.h
@@ -1,39 +1,43 @@
-/* ************************************************************************** */
-/*                                                                            */
-/*                                                        :::      ::::::::   */
-/*   header.h                                           :+:      :+:    :+:   */
-/*                                                    +:+ +:+         +:+     */
-/*   By: kychoi <marvin@42.fr>                      +#+  +:+       +#+        */
-/*                                                +#+#+#+#+#+   +#+           */
-/*   Created: 2021/08/15 12:41:41 by kychoi            #+#    #+#             */
-/*   Updated: 2021/08/15 12:45:53 by kychoi           ###   ########.fr       */
-/*                                                                            */
-/* ************************************************************************** */
-
-#ifndef HEADER_H
-#define	HEADER_H
-#define GAME_SIZE 4
+#ifndef HEADERFILE_H
+#define HEADERFILE_H
 
-#include <unistd.h>
+#include<unistd.h>
 typedef struct game_s
 {
-	int	grid[4][4];
-	int	view_points[4][4];
-}	t_game;
+	int grid[4][4];
+	int view_points[4][4];
+		/*
+		first index corresponds to the side: 0 = up 1 = down 2 = left 3 = right
+		second index corresponds to index each side
+		*/
+} game;
+
+#define GAME_SIZE 4
+#define NBR_CASE GAME_SIZE * GAME_SIZE
+
+//										check inputs
+int check_input(char* input);//returns 1 if inpu is correct, 0 otherwise
+
 
-int		check_input(char *input);
-t_game	init_game(char *input);
-int		*init_input_to_tab(int *tab);
+//										initialize game struct.
+game init_game(char* input);
+//returns the game struct with the view\_points initialized to the input and the grid with every cell initialized to 0
+int	*init_input_to_tab(int *tab);
 void	init_view_points(char *input, int view_points[][4]);
-int		*ft_atoi(char *str, int *res);
-/*solve game.*/
-int		solve(int x, int y, t_game *g);
-/*print game*/
-void	print_game(t_game g);
-void	print_game_with_view_points(t_game g);
-/*usefull*/
+int	*ft_atoi(char *str, int *res);
+
+//										solve game.
+int solve(int x, int y, game *g);
+//takes the adrres of the game struct, modifies it and returns 1 ifthe game has been solved, 0 otherwise
+
+
+//										print game
+void print_game(game g);//prints the gameon the command line, gets called if the previous funcion returns 1.
+void print_game_with_view_points(game g);
+
+//										usefull
 int		ft_strlen(char *str);
-int		is_number(char c);
 void	ft_putchar(char c);
 void	ft_putstr(char *str);
+int		is_number(char c); 
 #endif
diff --git a/init_game.c b/init_game.c
index 3ee186a..9be7372 100644
--- a/init_game.c
+++ b/init_game.c
@@ -1,23 +1,12 @@
-/* ************************************************************************** */
-/*                                                                            */
-/*                                                        :::      ::::::::   */
-/*   init_game.c                                        :+:      :+:    :+:   */
-/*                                                    +:+ +:+         +:+     */
-/*   By: kychoi <marvin@42.fr>                      +#+  +:+       +#+        */
-/*                                                +#+#+#+#+#+   +#+           */
-/*   Created: 2021/08/15 12:17:00 by kychoi            #+#    #+#             */
-/*   Updated: 2021/08/15 12:43:46 by kychoi           ###   ########.fr       */
-/*                                                                            */
-/* ************************************************************************** */
-
 #include "header.h"
+#include<stdio.h>
 
-t_game	init_game(char *av)
+game init_game(char* av)
 {
-	t_game	g;
-	int		x;
-	int		y;
-	int		index;
+	game g;
+	int x;
+	int	y;
+	int index;
 
 	x = -1;
 	while (++x < 4)
diff --git a/main.c b/main.c
index 8e0e21e..1a2ccd1 100644
--- a/main.c
+++ b/main.c
@@ -1,27 +1,16 @@
-/* ************************************************************************** */
-/*                                                                            */
-/*                                                        :::      ::::::::   */
-/*   main.c                                             :+:      :+:    :+:   */
-/*                                                    +:+ +:+         +:+     */
-/*   By: kychoi <marvin@42.fr>                      +#+  +:+       +#+        */
-/*                                                +#+#+#+#+#+   +#+           */
-/*   Created: 2021/08/15 12:17:34 by kychoi            #+#    #+#             */
-/*   Updated: 2021/08/15 12:42:07 by kychoi           ###   ########.fr       */
-/*                                                                            */
-/* ************************************************************************** */
+#include"header.h"
+#include<stdio.h>
 
-#include "header.h"
-
-int	main(int ac, char **av)
+int main(int ac, char **av)
 {
-	t_game	g;
+	game g;
 
 	if (ac != 2 || check_input(av[1]) == 0)
 	{
 		ft_putstr("Error\n");
 		return (1);
 	}
-	g = init_game(av[1]);
+	g = init_game(av[1]);;
 	if (solve(0, 0, &g) == 0)
 	{
 		ft_putstr("Error\n");
diff --git a/print_game.c b/print_game.c
index 2f1298b..f3c3411 100644
--- a/print_game.c
+++ b/print_game.c
@@ -1,18 +1,6 @@
-/* ************************************************************************** */
-/*                                                                            */
-/*                                                        :::      ::::::::   */
-/*   print_game.c                                       :+:      :+:    :+:   */
-/*                                                    +:+ +:+         +:+     */
-/*   By: kychoi <marvin@42.fr>                      +#+  +:+       +#+        */
-/*                                                +#+#+#+#+#+   +#+           */
-/*   Created: 2021/08/15 12:17:05 by kychoi            #+#    #+#             */
-/*   Updated: 2021/08/15 12:46:57 by kychoi           ###   ########.fr       */
-/*                                                                            */
-/* ************************************************************************** */
-
 #include "header.h"
 
-void	print_game(t_game g)
+void print_game(game g)
 {
 	int	x;
 	int	y;
@@ -30,3 +18,43 @@ void	print_game(t_game g)
 		ft_putchar('\n');
 	}
 }
+
+void print_game_with_view_points(game g)
+{
+	int	x;
+	int	y;
+
+	y = -1;
+	ft_putchar(' ');
+	ft_putchar(' ');
+	while (++y < 4)
+	{
+		ft_putchar('0' + g.view_points[0][y]);
+		ft_putchar(' ');
+	}
+	ft_putchar('\n');
+	y = -1;
+	while (++y < 4)
+	{
+		ft_putchar('0' + g.view_points[2][y]);
+		ft_putchar(' ');
+		x = -1;
+		while (++x < 4)
+		{
+			ft_putchar('0' + g.grid[x][y]);
+			ft_putchar(' ');
+		}
+		ft_putchar('0' + g.view_points[3][y]);
+		ft_putchar('\n');
+	}
+	ft_putchar(' ');
+	ft_putchar(' ');
+	y = -1;
+	while (++y < 4)
+	{
+		ft_putchar('0' + g.view_points[1][y]);
+		ft_putchar(' ');
+	}
+	ft_putchar('\n');
+	ft_putchar('\n');
+}
diff --git a/solve.c b/solve.c
index a6d449c..6843fdb 100644
--- a/solve.c
+++ b/solve.c
@@ -1,28 +1,20 @@
-/* ************************************************************************** */
-/*                                                                            */
-/*                                                        :::      ::::::::   */
-/*   solve.c                                            :+:      :+:    :+:   */
-/*                                                    +:+ +:+         +:+     */
-/*   By: kychoi <marvin@42.fr>                      +#+  +:+       +#+        */
-/*                                                +#+#+#+#+#+   +#+           */
-/*   Created: 2021/08/15 12:16:55 by kychoi            #+#    #+#             */
-/*   Updated: 2021/08/15 12:43:56 by kychoi           ###   ########.fr       */
-/*                                                                            */
-/* ************************************************************************** */
+#include"header.h"
+#include<stdio.h>
 
-#include "header.h"
 
-int	count_views_on_line(int y, int dir, t_game g)
+int count_views_on_line(int x, int y, int dir, game g)
 {
 	int	i;
-	int	limit;
+	int limit;
 	int	highest;
 	int	count;
 
 	i = (dir == 1) * 3;
 	limit = (dir == 0) * 5 - 1;
 	dir = 1 - (dir == 1) * 2;
+	//printf("count called, i: %d, limit: %d, dir: %d\n", i, limit, dir);
 	highest = g.grid[i][y];
+	//printf("highest: %d\n", highest);
 	count = 1;
 	while (i != limit)
 	{
@@ -31,44 +23,58 @@ int	count_views_on_line(int y, int dir, t_game g)
 			highest = g.grid[i][y];
 		i += dir;
 	}
+	//printf("count: %d\n\n", count);
 	return (count);
 }
 
-int	can_put_on_line(int x, int y, int val, t_game *g)
+int can_put_on_line(int x, int y, int val, game *g)
 {
-	int	is_leg;
-	int	view_left;
-	int	view_right;
+	int is_leg;
+	int view_left;
+	int view_right;
 
 	is_leg = 1;
+//	printf("called, (%d, %d), val: %d\n", x, y, val);
+	//check for duplicates
 	is_leg *= g->grid[(x + 1) % 4][y] != val;
 	is_leg *= g->grid[(x + 2) % 4][y] != val;
 	is_leg *= g->grid[(x + 3) % 4][y] != val;
+//	printf("%s duplicates\n", is_leg ? "no" : "found");
+
+	//check for view points
+	//printf("putting val at (%d, %d)\n", x, y);
 	g->grid[x][y] = val;
-	view_left = count_views_on_line(y, 0, *g);
-	view_right = count_views_on_line(y, 1, *g);
+	view_left = count_views_on_line(x, y, 0, *g);
+	view_right = count_views_on_line(x, y, 1, *g);
+	//printf("right count: %d, left count: %d\n", view_left, view_right);
 	if (x == 3)
 	{
 		is_leg *= view_left == g->view_points[2][y];
 		is_leg *= view_right == g->view_points[3][y];
+//		printf("view right: %d, view left %d\n",view_right, view_left);
+//		printf("view_point right: %d, view_point left %d\n",g->view_points[3][y], g->view_points[2][y]);
+//		printf("check_right: %d, check_left %d\n",view_right == g->view_points[3][y], view_left == g->view_points[2][y]);
 	}
 	else
 		is_leg *= view_left <= g->view_points[2][y];
+	//printf("move is%s legal\n", is_leg ? "" : " NOT");
 	g->grid[x][y] *= is_leg;
 	return (is_leg);
 }
 
-int	count_views_on_columm(int x, int dir, t_game g)
+int count_views_on_columm(int x, int y, int dir, game g)
 {
 	int	i;
-	int	limit;
+	int limit;
 	int	highest;
 	int	count;
 
 	i = (dir == 1) * 3;
 	limit = (dir == 0) * 5 - 1;
 	dir = 1 - (dir == 1) * 2;
+	//printf("count called, i: %d, limit: %d, dir: %d\n", i, limit, dir);
 	highest = g.grid[x][i];
+	//printf("highest: %d\n", highest);
 	count = 1;
 	while (i != limit)
 	{
@@ -77,22 +83,30 @@ int	count_views_on_columm(int x, int dir, t_game g)
 			highest = g.grid[x][i];
 		i += dir;
 	}
+	//printf("count: %d\n\n", count);
 	return (count);
 }
 
-int	can_put_on_columm(int x, int y, int val, t_game *g)
+int can_put_on_columm(int x, int y, int val, game *g)
 {
-	int	is_leg;
-	int	view_top;
-	int	view_down;
+	int is_leg;
+	int view_top;
+	int view_down;
 
 	is_leg = 1;
+//	printf("called, (%d, %d), val: %d\n", x, y, val);
+	//check for duplicates
 	is_leg *= g->grid[x][(y + 1) % 4] != val;
 	is_leg *= g->grid[x][(y + 2) % 4] != val;
 	is_leg *= g->grid[x][(y + 3) % 4] != val;
+//	printf("%s duplicates\n", is_leg ? "no" : "found");
+
+	//check for view points
+	//printf("putting val at (%d, %d)\n", x, y);
 	g->grid[x][y] = val;
-	view_top = count_views_on_columm(x, 0, *g);
-	view_down = count_views_on_columm(x, 1, *g);
+	view_top = count_views_on_columm(x, y, 0, *g);
+	view_down = count_views_on_columm(x, y, 1, *g);
+	//printf("right count: %d, left count: %d\n", view_left, view_right);
 	if (y == 3)
 	{
 		is_leg *= view_top == g->view_points[0][x];
@@ -100,19 +114,23 @@ int	can_put_on_columm(int x, int y, int val, t_game *g)
 	}
 	else
 		is_leg *= view_top <= g->view_points[0][x];
+	//printf("move is%s legal\n", is_leg ? "" : " NOT");
 	g->grid[x][y] *= is_leg;
 	return (is_leg);
 }
 
-int	solve(int x, int y, t_game *g)
+int	solve(int x, int y, game *g)
 {
 	int	val;
 
 	val = 1;
 	while (val < 5)
 	{
+//		printf("(%d, %d) val: %d, line: %d, columm: %d\n", x, y, val, can_put_on_line(x, y, val, g), can_put_on_columm(x, y, val, g));
 		if (can_put_on_line(x, y, val, g) && can_put_on_columm(x, y, val, g))
 		{
+//			print_game_with_view_points(*g);
+//			printf("next x: %d\n", (x + 1) % 4);
 			if ((x == 3 && y == 3) || solve((x + 1) % 4, y + (x + 1) / 4, g))
 				return (1);
 		}
diff --git a/useful.c b/useful.c
index 248d4d0..92f0041 100644
--- a/useful.c
+++ b/useful.c
@@ -1,15 +1,3 @@
-/* ************************************************************************** */
-/*                                                                            */
-/*                                                        :::      ::::::::   */
-/*   useful.c                                           :+:      :+:    :+:   */
-/*                                                    +:+ +:+         +:+     */
-/*   By: kychoi <marvin@42.fr>                      +#+  +:+       +#+        */
-/*                                                +#+#+#+#+#+   +#+           */
-/*   Created: 2021/08/15 12:17:20 by kychoi            #+#    #+#             */
-/*   Updated: 2021/08/15 12:17:21 by kychoi           ###   ########.fr       */
-/*                                                                            */
-/* ************************************************************************** */
-
 #include "header.h"
 
 int	ft_strlen(char *str)
